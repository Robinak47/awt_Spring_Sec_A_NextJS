

export default function AddUser() {
    return (
        <div>
            <h1>Add User</h1>
            <p>This is the Add User page. Here you can add new users to the system.only for admin</p>
        </div>
    );
}